# CLAUDE.md

This repo is Arly Trenck's knowledge base for the `/arly` skill. Read `README.md`, then `REFRESH.md`.

- To refresh the knowledge base, follow "Doing a refresh" in `REFRESH.md`.
- A refresh edits only `OPINIONS.md`, `TOOLS.md` and `VOICE.md`, plus `state/baseline.state`.
- Use only public sources. Never private repositories, unpublished drafts, or employer material.
- No em dashes anywhere. `scripts/check.sh` enforces it, and it must pass before a commit.
- No AI attribution in commits or pull requests: no `Co-Authored-By`, no "Generated with" footer.
- Commit and push only when asked.
